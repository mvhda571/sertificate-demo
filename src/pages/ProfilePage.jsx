import { useState } from 'react'
import { Bell, Medal, Send, Trash2, User } from 'lucide-react'
import { useForm } from 'react-hook-form'
import { toast } from 'sonner'
import { useUserStore } from '../store/useUserStore'
import { useTestStore } from '../store/useTestStore'

const leaders = { Kunlik: [['Aziza',980],['Muslima',940],['Jahongir',890]], Haftalik: [['Muslima',6240],['Akmal',5910],['Nilufar',5780]], Oylik: [['Nilufar',24800],['Muslima',23600],['Akmal',22140]] }
export function ProfilePage() {
  const [period,setPeriod] = useState('Haftalik'); const {profile,points,updateProfile,notifications,toggleNotifications} = useUserStore(); const {errorLog,clearErrors}=useTestStore()
  const {register,handleSubmit,formState:{errors}} = useForm({defaultValues:profile})
  const submit = data => { updateProfile(data); toast.success('Profil saqlandi') }
  return <div className="space-y-6"><section className="hero-panel flex flex-wrap items-center gap-4"><span className="grid h-16 w-16 place-items-center rounded-full bg-gradient-to-br from-blue-600 to-emerald-500 text-xl font-black text-white">MK</span><div className="flex-1"><p className="eyebrow">Shaxsiy kabinet</p><h1 className="page-title">{profile.name}</h1><p className="mt-1 text-sm text-slate-500">{points} XP · Maqsad: {profile.targetGrade}</p></div><button onClick={toggleNotifications} className={`btn-secondary ${notifications ? 'text-emerald-600' : ''}`}><Bell/> Telegram {notifications ? 'yoqilgan' : 'o‘chiq'}</button></section><div className="grid gap-6 xl:grid-cols-2"><form onSubmit={handleSubmit(submit)} className="card-panel p-6"><div className="flex items-center gap-2"><User className="text-blue-600"/><h2 className="font-bold">Profil sozlamalari</h2></div><div className="mt-5 space-y-4"><Field label="Ism" error={errors.name?.message}><input {...register('name',{required:'Ism majburiy',minLength:{value:3,message:'Kamida 3 ta belgi'}})} className="form-input"/></Field><Field label="Telegram username" error={errors.telegram?.message}><input placeholder="@username" {...register('telegram',{pattern:{value:/^$|^@[a-zA-Z0-9_]{5,}$/,message:'@username formatida kiriting'}})} className="form-input"/></Field><Field label="Maqsad daraja"><select {...register('targetGrade')} className="form-input"><option>A+</option><option>A</option><option>B+</option><option>B</option></select></Field><button className="btn-primary w-full"><Send/> Saqlash va botga ulash</button></div></form><div className="card-panel p-6"><div className="flex justify-between"><div><p className="eyebrow">Top talabalar</p><h2 className="mt-1 font-bold">Leaderboard</h2></div><Medal className="text-orange-500"/></div><div className="mt-5 flex gap-1 rounded-xl bg-slate-100 p-1 dark:bg-slate-800">{Object.keys(leaders).map(p => <button key={p} onClick={()=>setPeriod(p)} className={`flex-1 rounded-lg py-2 text-xs font-bold ${period===p?'bg-white shadow dark:bg-slate-700':''}`}>{p}</button>)}</div><div className="mt-4 space-y-2">{leaders[period].map(([name,score],i)=><div key={name} className="flex items-center justify-between rounded-xl bg-slate-50 p-3 dark:bg-slate-800"><span><b className="mr-3 text-slate-400">#{i+1}</b>{name}</span><b>{score} XP</b></div>)}</div></div></div><section className="card-panel p-6"><div className="flex items-center justify-between"><div><p className="eyebrow text-orange-500">My Mistakes</p><h2 className="mt-1 text-xl font-bold">Xatolar tahlili</h2></div><button onClick={clearErrors} className="icon-button"><Trash2/></button></div>{errorLog.length ? <div className="mt-5 grid gap-3">{errorLog.map((q,i)=><details key={`${q.id}-${i}`} className="rounded-xl border border-slate-200 p-4 dark:border-slate-700"><summary className="cursor-pointer font-semibold">{q.text}</summary><p className="mt-3 text-sm text-slate-500">{q.explanation}</p><a href="https://www.youtube.com/results?search_query=matematika+darslari" target="_blank" rel="noreferrer" className="mt-3 inline-block text-sm font-bold text-blue-600">Video yechimni ko‘rish →</a></details>)}</div>:<p className="mt-6 text-center text-sm text-slate-500">Test yakunlangach xato savollar tushuntirishlari shu yerda saqlanadi.</p>}</section></div>
}
function Field({label,error,children}) { return <label className="block"><span className="mb-2 block text-sm font-semibold">{label}</span>{children}{error&&<span className="mt-1 block text-xs text-red-500">{error}</span>}</label> }
